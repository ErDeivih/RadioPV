# RadioPV · dependencias del backend y del recolector
#
# ⚠️ Este fichero lo lee pip: los comentarios SIEMPRE con '#'.
#    Ojo con el ';' — pip lo interpreta como separador de *environment marker*
#    (PEP 508, estilo "paquete; python_version>'3.10'") y falla con:
#        InvalidMarker: Expected a marker variable or quoted string

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
email-validator>=2.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]>=3.3
python-multipart>=0.0.9

# Solo para Postgres (con SQLite no hace falta, pero se deja por si se migra)
psycopg2-binary>=2.9

# Worker (python -m app.workers)
apscheduler>=3.10

# Migraciones de esquema
alembic>=1.13

# Pruebas
pytest>=8.0
httpx>=0.27
